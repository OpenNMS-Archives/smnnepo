<blueprint
	xmlns="http://www.osgi.org/xmlns/blueprint/v1.0.0"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" 
	xmlns:cm="http://aries.apache.org/blueprint/xmlns/blueprint-cm/v1.1.0"
	xmlns:ext="http://aries.apache.org/blueprint/xmlns/blueprint-ext/v1.1.0"
	xsi:schemaLocation="
		http://www.osgi.org/xmlns/blueprint/v1.0.0 
		http://www.osgi.org/xmlns/blueprint/v1.0.0/blueprint.xsd

		http://aries.apache.org/blueprint/xmlns/blueprint-cm/v1.1.0
		http://aries.apache.org/schemas/blueprint-cm/blueprint-cm-1.1.0.xsd

		http://aries.apache.org/blueprint/xmlns/blueprint-ext/v1.1.0
		http://aries.apache.org/schemas/blueprint-ext/blueprint-ext-1.1.xsd
">

  <!-- Configuration properties -->
  <cm:property-placeholder id="properties" persistent-id="org.opennms.netmgt.sampler.persister" update-strategy="none">
    <cm:default-properties>
      <cm:property name="brokerName" value="persisterBroker"/>
    </cm:default-properties>
  </cm:property-placeholder>

  <!-- Wire in a dispatcher that will route SampleSet messages into the Camel context -->
  <bean id="activemqDispatcher" class="org.opennms.netmgt.api.sample.support.DefaultSampleSetDispatcher">
    <argument value="direct:sendSampleSet"/>
  </bean>

  <service ref="activemqDispatcher" interface="org.opennms.netmgt.api.sample.SampleSetDispatcher">
    <service-properties>
      <entry key="impl" value="activemq"/>
    </service-properties>
  </service>

  <camelContext id="activemqDispatcherCamelContext" xmlns="http://camel.apache.org/schema/blueprint">

    <!-- Send SampleSets over ActiveMQ -->
    <route id="sendSampleSet">
      <from uri="direct:sendSampleSet" />
      <to uri="activemq:sampleSet" />
    </route>

  </camelContext>

  <bean id="activemq" class="org.apache.activemq.camel.component.ActiveMQComponent">
    <property name="brokerURL" value="vm://localhost?broker.brokerName=${brokerName}&amp;broker.persistent=false" />
  </bean>

<!--
  <camelContext xmlns="http://camel.apache.org/schema/blueprint">
    <!- - Camel route to feed the ActiveMQ inbox queue once per second - ->
    <route>
      <from uri="timer:foo?period=1s"/>
      <transform>
        <simple>uzbekistan sending message at ${date:now:yyyy-MM-dd HH:mm:ss}</simple>
      </transform>
      <to uri="activemq:queue:texasTrigger"/>
    </route>
    
    <route>
      <from uri="activemq:queue:uzbekTrigger"/>
      <transform>
        <simple>Routing Message at ${date:now:yyyy-MM-dd HH:mm:ss} to texas: ${body}</simple>
      </transform>
      <to uri="activemq:queue:texasIn"/>
    </route>
    
    <route>
      <from uri="activemq:queue:uzbekIn"/>
      <to uri="file:///tmp/messages" />
    </route>
    
  </camelContext>
-->

</blueprint>
